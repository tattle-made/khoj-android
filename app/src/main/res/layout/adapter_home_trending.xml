<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/secondaryTextColor"
    android:paddingBottom="@dimen/page_margin">

    <FrameLayout
        android:id="@+id/layoutImage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toStartOf="@+id/tvTrendingHead"
        app:layout_constraintHorizontal_weight="0.25"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/imgTrending"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:contentDescription="@string/home" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvTrendingHead"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:contentDescription="@string/home"
        android:textColor="@color/primaryTextColor"
        android:textSize="16sp"
        app:layout_constraintHorizontal_weight="0.75"
        app:layout_constraintStart_toEndOf="@+id/layoutImage"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvTrendingDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/top_margin"
        android:contentDescription="@string/home"
        android:textColor="@color/primaryTextColor"
        app:layout_constraintStart_toStartOf="@+id/tvTrendingHead"
        app:layout_constraintTop_toBottomOf="@+id/tvTrendingHead" />

</androidx.constraintlayout.widget.ConstraintLayout>